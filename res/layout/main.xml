<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/RelativeLayout01"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!--todo move this button little bit higher-->
    <view
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:padding="250dp"
        android:text="@string/createTask"
        android:id="@+id/createTaskButton"
        android:layout_centerInParent="true"
        android:background="@drawable/roundedbutton"
        android:contentDescription="@string/centerButtonDescription"
        class="com.timetracker.ui.MainButtonView"
        android:scrollbars="vertical"
        android:fadingEdge="vertical" />
    <!--todo add some valid description-->

</RelativeLayout>
